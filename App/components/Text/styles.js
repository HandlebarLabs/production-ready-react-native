import { StyleSheet } from 'react-native';
import colors from '../../config/colors';

export default StyleSheet.create({
  smallText: {
    color: colors.$white,
    textAlign: 'center',
    fontSize: 12,
  },
});
